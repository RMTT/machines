resources:
  - namespace.yaml

namespace: authentik
helmCharts:
  - name: authentik
    releaseName: authentik
    namespace: authentik
    repo: https://charts.goauthentik.io
    version: 2025.2.0
    valuesFile: values.yaml.dec
